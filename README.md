# BigCollatz

BigCollatz is a small experimental Python program for evaluating exact Collatz
trajectories from 1000-digit starting integers. For `n > 0`, one unaccelerated
step is `n / 2` when even and `3n + 1` when odd.

The active runner is deliberately sequential. By default, an experiment tests
exactly 10,000 distinct candidates generated by `S0-uniform-deterministic`.
It keeps trajectory lengths for statistics and only the current top ten result
records in memory; it does not retain every starting integer or result record.

## Run

```bash
python -m unittest discover -v
python -m bigcollatz run e001
```

Do not launch the second command until a full experiment is intended. It writes
only `summary.json`, `summary.md`, and `top_10.json` under `results/e001/`, then
merges the winners into `results/global_top_10.json`. Machine-readable top-ten
files preserve complete integers; Markdown abbreviates them except for the best
starting integer.

There is no sharding, checkpointing, scheduler, persistent cache, database, or
worker orchestration. See `EXPERIMENTS.md` for the historical pilot and
`STRATEGIES.md` for strategy notes.
